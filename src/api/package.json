{
  "name": "app",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "migrate": "npx knex --knexfile=./db/knexfile migrate:latest",
    "migrate:rollback": "npx knex --knexfile=./db/knexfile migrate:rollback",
    "migration": "knex --knexfile=./db/knexfile migrate:make",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "knex": "^0.20.8",
    "pg": "^7.17.1"
  }
}
