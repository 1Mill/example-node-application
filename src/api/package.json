{
  "name": "app",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev:migrate": "npx knex migrate:latest",
    "dev:migrate:rollback": "npx knex migrate:rollback",
    "dev:migration": "knex migrate:make",
    "dev:seed": "npx knex seed:run",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "express": "^4.17.1",
    "knex": "^2.4.0",
    "pg": "^7.17.1"
  }
}
